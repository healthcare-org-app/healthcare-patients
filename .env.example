DATABASE_URL=postgresql://healthcare:healthcare_dev@localhost:5432/patients
KAFKA_BOOTSTRAP=localhost:29092
LOG_LEVEL=INFO

# Auth (dev)
JWT_SECRET=dev-secret-do-not-use-in-prod
JWT_ALGORITHM=HS256
AUTH_DISABLED=1

# Peer service URLs (local dev — override with SERVICE_URL_* if needed)
PORT_IDENTITY_SERVICE=8001
PORT_AUTH_SERVICE=8002
PORT_AUDIT_LOG_SERVICE=8007
PORT_NOTIFICATIONS_SERVICE=9000
